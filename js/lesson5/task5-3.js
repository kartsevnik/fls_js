// Enter the required data
let table = `<table style="text-align: center">`

for (let num = 1; num <= 7; num++) {
    table = table + `<td style="border: 1px solid #fff; display:inline">${num}</td>`
}

table = table + `</table>`

document.write(table)
// Determining the result



